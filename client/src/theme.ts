import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#a19f9a",
    },
    primary: {
      main: "#4a4343",
    },
  },
});

export default theme;
